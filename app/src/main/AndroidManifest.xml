<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <application
        android:name=".MononoteApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_mononote"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_mononote"
        android:supportsRtl="true"
        android:theme="@style/Theme.Mononote"
        android:resizeableActivity="true"
        tools:targetApi="35">

        <!-- Main Single-Canvas Editor Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:resizeableActivity="true"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.Mononote">
            
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Support direct sharing of text into Mononote -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>

            <!-- 🎙️ Google Assistant & Gemini Voice Note Intent Filters -->
            <intent-filter>
                <action android:name="com.google.android.gms.actions.CREATE_NOTE" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.VOICE" />
                <data android:mimeType="*/*" />
            </intent-filter>

            <intent-filter>
                <action android:name="android.intent.action.CREATE_NOTE" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.VOICE" />
                <data android:mimeType="*/*" />
            </intent-filter>

            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />
        </activity>

        <!-- Live Card Action Broadcast Receiver (Lock Screen & Remote Broadcasts) -->
        <receiver
            android:name=".live.LiveActionReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.minimalist.mononote.ACTION_COMPLETE_NOTE" />
                <action android:name="com.minimalist.mononote.ACTION_COPY_NOTE" />
                <action android:name="com.minimalist.mononote.ACTION_STOP_LIVE" />
                <action android:name="com.minimalist.mononote.ACTION_APPEND_NOTE" />
                <action android:name="com.minimalist.mononote.ACTION_SET_NOTE" />
            </intent-filter>
        </receiver>

        <!-- Quick Settings Tile (Pull-down Control Center toggle) -->
        <service
            android:name=".tile.MononoteTileService"
            android:exported="true"
            android:icon="@drawable/ic_mononote"
            android:label="@string/tile_name"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <!-- Glance Home Screen AppWidget Receiver -->
        <receiver
            android:name=".widget.MononoteWidgetReceiver"
            android:exported="true"
            android:label="@string/widget_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/glance_widget_info" />
        </receiver>

        <!-- Floating Dynamic Capsule Service (Optional Overlay Pill) -->
        <service
            android:name=".live.FloatingCapsuleService"
            android:exported="false" />

    </application>

</manifest>
